name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted]

concurrency:
  group: claude-review-${{ github.event.pull_request.number || github.event.issue.number }}
  cancel-in-progress: true

jobs:
  claude-review:
    # PR이 생성되거나 업데이트되거나, @claude 멘션이 있을 때 실행
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude'))
    runs-on: ubuntu-latest
    timeout-minutes: 15

    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Claude Code Action
        uses: anthropics/claude-code-action@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          show_full_output: true
          use_sticky_comment: true
          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            ## 작업 단계

            1. **이전 리뷰 확인**: `gh pr view ${{ github.event.pull_request.number }} --comments` 명령으로 이전 코멘트를 확인하세요.

            2. **변경사항 분석**: 최신 커밋에서 이전에 지적된 이슈들이 수정되었는지 확인하세요.

            3. **Follow-up 코멘트 작성**:
               - 수정된 이슈가 있다면, 해당 이슈를 언급하며 "✅ 해결됨: [이슈 설명]" 형식으로 follow-up 코멘트를 작성하세요.
               - 새로운 이슈가 발견되면 인라인 코멘트로 추가하세요.

            4. **요약 코멘트**: 전체 리뷰 상태를 요약한 코멘트를 `gh pr comment`로 작성하세요.
               - 해결된 이슈 목록
               - 남은 이슈 목록
               - 새로 발견된 이슈 목록

            Use `gh pr comment` for top-level feedback.
            Use `mcp__github_inline_comment__create_inline_comment` to highlight specific code issues.
            Only post GitHub comments - don't submit review text as messages.

            다음 기준에 따라 리뷰해주세요:

            ## 리뷰 기준 (CLAUDE.md 참고)
            - [Critical] 버그, 보안 취약점, 타입 안정성 문제, 메모리 누수
            - [Warning] 아키텍처 위반, 성능 이슈, 에러 핸들링 누락
            - [Suggestion] 코드 개선 제안, 가독성 향상

            ## 출력 형식
            PR에 코멘트로 작성:
            ```markdown
            ## 🤖 Claude Code Review

            ### 📊 Summary
            (변경사항 2-3줄 요약)

            ### 🔍 Issues Found

            #### 🔴 Critical
            - 파일명:줄번호 - 이슈 설명 및 해결 방법

            #### 🟡 Warning
            - 파일명:줄번호 - 이슈 설명 및 해결 방법

            #### 💡 Suggestion
            - 파일명:줄번호 - 제안 사항

            ### ✅ Good Points
            - 잘된 부분들

            ### 📝 Overall
            Status: [✅ Approved | 🔴 Changes Requested]
            ```

            반드시 발견한 모든 이슈를 구체적으로 지적하고, 파일명과 줄번호를 명시해주세요.
