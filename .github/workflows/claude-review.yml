name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted]

concurrency:
  group: claude-review-${{ github.event.pull_request.number || github.event.issue.number }}
  cancel-in-progress: true

jobs:
  claude-review:
    # PR이 생성되거나 업데이트되거나, @claude 멘션이 있을 때 실행
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude'))
    runs-on: ubuntu-latest
    timeout-minutes: 15

    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Claude Code Action
        uses: anthropics/claude-code-action@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          show_full_output: true
          use_sticky_comment: true
          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh api graphql:*)"
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number || github.event.issue.number }}
            EVENT TYPE: ${{ github.event_name }}
            USER COMMENT: ${{ github.event.comment.body || github.event.review.body || 'N/A' }}
            COMMENT URL: ${{ github.event.comment.html_url || github.event.review.html_url || 'N/A' }}

            ## 이벤트 유형별 처리

            ### @claude 멘션된 경우 (issue_comment, pull_request_review_comment, pull_request_review)

            사용자가 "@claude [요청]" 형태로 멘션했습니다. USER COMMENT를 확인하세요:

            1. **코멘트 내용 확인**: 사용자가 무엇을 요청했는지 확인
               - "리졸브해줘", "resolve", "이거 해결됐어" → 해당 thread를 resolved로 마크
               - "다시 리뷰해줘", "review again" → 해당 코드 부분을 다시 리뷰
               - 질문이나 요청 → 답변 제공

            2. **요청 수행**:
               - **Resolve 요청**:
                 - 코드가 실제로 수정되었는지 확인
                 - 수정되었다면: GraphQL mutation으로 thread resolve + "✅ 확인했습니다. [이슈]가 해결되었네요!" 답장
                 - 아직 문제 있다면: "🔴 아직 [문제]가 남아있습니다: [설명]" 답장
               - **리뷰 요청**: 해당 파일/라인 다시 분석 후 답장
               - **질문**: 관련 코드를 분석해서 답변

            3. **답장**: 반드시 사용자가 멘션한 코멘트에 답장하세요
               - `gh pr comment` 명령으로 답변 (PR 전체 코멘트로 달립니다)
               - 형식: "@[사용자이름] [답변 내용]" 형태로 멘션해서 답변

            ### PR 자동 리뷰 (pull_request 이벤트)

            ## 작업 단계

            1. **이전 리뷰 스레드 확인**: GraphQL API로 review threads 조회
               ```bash
               gh api graphql -f query='query { repository(owner: "${{ github.repository_owner }}", name: "${{ github.event.repository.name }}") { pullRequest(number: ${{ github.event.pull_request.number }}) { reviewThreads(first: 50) { nodes { id isResolved comments(first: 1) { nodes { body path line } } } } } }'
               ```

            2. **변경사항 분석**:
               - `git diff` 명령으로 이번 커밋에서 실제로 변경된 파일과 라인만 확인하세요.
               - **중요**: 변경되지 않은 파일은 리뷰하지 마세요.

            3. **중복 방지 규칙 (매우 중요)**:
               - ❌ **절대 금지**: 이미 인라인 코멘트가 있는 파일:라인에 중복 코멘트 작성
               - ❌ **절대 금지**: 이미 리뷰한 이슈를 다시 언급 (단, 수정되었다면 resolve)
               - ✅ **허용**: 새로 변경된 라인의 새로운 이슈만 코멘트
               - ✅ **허용**: 이전에 지적되었지만 수정된 이슈는 resolve + 확인 코멘트

            4. **해결된 스레드 Resolve**:
               - 수정된 이슈가 있다면, GraphQL mutation으로 해당 thread를 resolve하세요:
               ```bash
               gh api graphql -f query='mutation { resolveReviewThread(input: {threadId: "THREAD_ID"}) { thread { isResolved } } }'
               ```
               - 그리고 해당 스레드에 "✅ 해결 확인됨: [수정 내용 설명]" 코멘트를 추가하세요.

            5. **새 이슈 리뷰**:
               - **오직 새로 변경된 코드에서 발견된 새로운 이슈만** 인라인 코멘트로 추가하세요.
               - 변경되지 않은 기존 코드의 이슈는 무시하세요.

            6. **요약 코멘트**:
               - **변경사항이 없거나 새로운 이슈가 없으면 요약 코멘트를 작성하지 마세요.**
               - 변경사항이 있을 때만 전체 리뷰 상태를 요약한 코멘트를 `gh pr comment`로 작성하세요:
                 - ✅ 해결된 이슈 목록 (resolved로 마크됨)
                 - 🟡 남은 이슈 목록 (이미 코멘트된 것은 제외)
                 - 🆕 새로 발견된 이슈 목록 (이번 커밋에서만)

            Use `gh pr comment` for top-level feedback.
            Use `mcp__github_inline_comment__create_inline_comment` to highlight specific code issues.
            Only post GitHub comments - don't submit review text as messages.

            다음 기준에 따라 리뷰해주세요:

            ## 리뷰 기준 (CLAUDE.md 참고)
            - [Critical] 버그, 보안 취약점, 타입 안정성 문제, 메모리 누수
            - [Warning] 아키텍처 위반, 성능 이슈, 에러 핸들링 누락
            - [Suggestion] 코드 개선 제안, 가독성 향상

            ## 출력 형식
            PR에 코멘트로 작성:
            ```markdown
            ## 🤖 Claude Code Review

            ### 📊 Summary
            (변경사항 2-3줄 요약)

            ### 🔍 Issues Found

            #### 🔴 Critical
            - 파일명:줄번호 - 이슈 설명 및 해결 방법

            #### 🟡 Warning
            - 파일명:줄번호 - 이슈 설명 및 해결 방법

            #### 💡 Suggestion
            - 파일명:줄번호 - 제안 사항

            ### ✅ Good Points
            - 잘된 부분들

            ### 📝 Overall
            Status: [✅ Approved | 🔴 Changes Requested]
            ```

            반드시 발견한 모든 이슈를 구체적으로 지적하고, 파일명과 줄번호를 명시해주세요.
